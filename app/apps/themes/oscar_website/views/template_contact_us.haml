%section#contact
  .body.margin-top-contact
    - title = @post.the_title
    = render 'partials/bread_crumb_contact', locals: {menu: title, sub_menu: title}
    .main{:role => "main"}
      = render 'partials/contact_us'