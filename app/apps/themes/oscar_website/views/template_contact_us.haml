%section#contact
  .body.marginTopContact
    - title = @post.the_title
    = render 'partials/bread_crumb', locals: {menu: title, sub_menu: title}
    .main{:role => "main"}
      = render 'partials/contact_us'